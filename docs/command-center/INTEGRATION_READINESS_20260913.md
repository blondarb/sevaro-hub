# Command Center integration readiness — September 13, 2026

## Delivered and held

The original owner-private Site is published. Its verified owner can read the
synthetic snapshot, and its authenticated browser tools returned that same snapshot
inside the native Codex Voice conversation. Item two resolved to Harbor dependency.
This is narrower than proving every ChatGPT web/mobile Voice client supports it.

The next source candidate adds host-side Asana/GitHub metadata adapters, a strict
Claude export reader, repaired sync-health ingestion, one expiring immutable Today
snapshot, runtime delivery checks and source-linked numbered read tools. It is not
a larger dashboard. This candidate is not yet saved or deployed in Sites.

Local metadata reads from Asana and GitHub succeeded and produced a private pending
proposal. No real proposal was uploaded to Sites or committed to Git. Claude exports
and the repaired monitor schema are unavailable; they are explicitly marked so.
Historical portfolio JSON is not used as a live feed.

The independent review found and this candidate fixes hidden Today records in tool
responses, premature review labels, unsafe private-file placement, empty source
allowlists, invalid derived views, arbitrary build overwrites and item 100 handling.
Twenty-six synthetic checks pass, including those regressions. Hosted access and
transport acceptance remain separate from these tests.

## Remaining acceptance gates

| Gate | Evidence / remaining work |
| --- | --- |
| Owner-private publication | Original synthetic version is live; owner binding verified after user-opened sign-in diagnostic |
| Native conversational retrieval | Actual authenticated tools returned the published fixed snapshot during Codex Voice; other Voice clients untested |
| Unauthorized access | Local identity tests pass. Hosted anonymous, second-account and spoofed-header denial remain unaccepted. Edge error 1010 is not proof of Worker authorization |
| Runtime delivery | New candidate supports one protected setting, exact digest and expiry; synthetic A→B hosted release and old-pin refusal are not yet exercised |
| Data retention | Sites setting-revision retention/purge and absence of payloads from platform logs need confirmation before executive content |
| Exact content approval | Pending draft is runtime-forbidden. Real mode additionally requires an independent digest-bound, time-limited approval receipt |
| Claude exports | Contract below is prepared; no claim that existing routines adopted it or generated a reviewed feed |
| Refresh | Manual/on-demand candidate, not a live unattended sync. Expired context becomes unavailable |

The original publishing exception allowed a short-lived credential in one recorded
source-upload command. It does not grant a standing exception for subsequent uploads.
Prepare and verify the new candidate before seeking any further narrowly scoped
transfer exception if no protected source upload becomes available.

## Existing Claude routines and connection contract

Read-only inventory of definitions, not proof of actual installed schedules or
successful runs. Sources: sevaro-ops `scheduled-tasks/manifest.json`, task SKILL files,
Labs coordination design, and existing context scripts. No Slack access, raw mail,
meeting transcripts or private run outputs were read for this inventory.

| Existing routine | Existing inputs / outputs | Reliability gap relevant to Hub |
| --- | --- | --- |
| Labs coordination | Fyxer, Outlook Sent Items, Asana, SharePoint and Cowork artifacts; existing data.js/HANDOFF ownership and coordination updates | Per-machine watermarks; cold-start lookback; missing transcript coverage must be explicit |
| Weekly Labs status | Asana portfolio and existing team Slack update | Cadence is defined; last successful execution needs a receipt |
| Asana session sync | Local queue/session handoffs; last-run and health artifacts | Definition is not an installed runner; legacy caller retirement remains required |
| Morning brief | Existing local calendar, Asana, Git and health | Shell success alone is insufficient because the script can exit zero with alerts; calendar permission can fail |
| Team digest | Existing team-only digest and Slack delivery path | Posting is approval-gated/disabled pending sample; validate application success, not HTTP status alone |
| Meeting intake and beta/blocker follow-ups | Existing Fyxer and Claude Slack routines | No independent completion receipt in some definitions; missing meetings are not an empty feed |
| Feedback triage and Clara feedback | Existing reviewed feedback reports and coordination | Definition-only evidence for some jobs; watermark truncation can lose coverage |
| Repository audit, app health, workspace review and showcase sync | Existing code/health checks and notifications | Configured/enabled differs from observed successful run |
| Leadership prep reminder | Existing reminder path | Potential overlap with local scheduling remains unresolved |
| Temporary channel poller | Disabled definition | Keep disabled; do not create a replacement poller |

Claude retains Outlook/email, Fyxer/meetings, its existing Slack access, documents
and Labs routines. Codex does not acquire Slack access. Preserve the documented
Claude data.js / other-agent index.html ownership contract; neither file is changed.

Adopt the `command-center/README.md` feed schema only at the end of an existing
routine's approved workflow. Place reviewed exports in the private Command Center
handoff directory, never this public repository. Suggested fixed filenames are
`claude-coordination.json`, `claude-replies.json` and `claude-calendar.json`.

Export only actionable decisions, commitments, blockers, relevant meeting preparation
and source-linked response intent. No email bodies, raw transcripts, clinical data,
PHI, attachments, unrestricted Slack content, credentials or copied source prompts.
Existing approved draft replies may be linked; importing their text requires separate
content review. Only exact approved HTTPS hosts are accepted.

Each feed supplies its source ID, original observation/expiry, available/unavailable
state, fixed failure code and curated items. Preserve source revision and deep link.
Never invent an owner, priority, stage, due date, successful run or next event. Failure
means unavailable; a schedule definition alone never means the job ran. Duplicates or
conflicting revisions stop assembly rather than silently winning.

ChatGPT handles executive prioritization and decision synthesis against these same
pinned items. Codex owns adapters, schema checks, runtime reliability and tests.
Steve's spoken instructions remain proposed actions. There is no sending, Asana
editing or cross-agent execution endpoint in this phase.

## Asana decisions and cutover

- FPPE: Asana Validate versus coordination Graduate; Steve decides whether a current
  stage change is appropriate. Keep Asana authoritative until an approved write.
- SDNE: Asana Explore versus coordination Validate; same decision gate. A product
  name containing DONE is not evidence that its task is complete.
- Preserve the 43 historical queue items as audit material. Do not replay historical
  comments or stage changes. Refresh only proposals for changes still needed.
- Retire the installed `com.sevaro.asana-repo-snapshot` caller (06:40 local direct
  Team Ops note writes) before the repaired writer becomes authoritative. Adopt the
  queue-only session/weekly adapters and verify other hosts/callers too.

Merge sequence remains: review/merge ops repair; review/merge context adapters and
adopt them; retire the legacy caller and verify all known hosts; install repaired
monitor and verify scheduled receipts; read fresh Asana state and approve exact
remaining payloads; enable one writer and verify readback. Do not merge automatically.
The repaired monitor separates actionable items from the 43 audit-only entries.

The local durable Control Plane broker is unavailable in this runtime. Repository
and private shared handoffs remain the coordination path; no broker was started.
