# Handoff — 2026-03-15

## What was done

### Feedback Admin Dashboard
- Added Cognito authentication to the Hub (login page, AuthProvider, NavBar, JWT verification)
- Admin-only access controlled by `ADMIN_EMAILS` env var (default: steve@sevaro.com)
- Protected all `/feedback` routes — unauthenticated users redirect to login

### Feedback Status Management
- Review status tracking: open → in_progress → resolved → dismissed
- Resolution notes with resolved-by attribution
- Status badges on feedback list and detail pages

### Delete Feedback
- Full cleanup: DynamoDB record + S3 audio/screenshots/transcript
- Confirmation modal before deletion
- Lambda updated with S3 ListObjects + DeleteObject cleanup

### AI Pattern Analysis
- `/feedback/analyze` page with configurable time range (7/14/30/90 days)
- Bedrock Sonnet analyzes unresolved feedback for themes, severity, frequency, recommendations
- Results display: summary, top priority, theme cards with severity badges

### Email Notifications
- SES domain `neuroplans.app` verified with DKIM
- Sends from `feedback@neuroplans.app` when feedback is marked resolved
- HTML email template with feedback summary and resolution note

### Infrastructure
- Created `SevaroHub-AmplifySSR` IAM role (SES + Bedrock + SSM permissions)
- Set Amplify env vars: Cognito pool/client, admin emails, feedback API URL
- Added PATCH to API Gateway CORS methods
- Deployed updated Lambda with PATCH/DELETE handlers

## Decisions
- Used `neuroplans.app` for SES (not `sevaro.com`) since Route 53 only manages neuroplans.app DNS
- Hub Cognito app client: `7t8bjj2fjkvtu081qhledc627a` (same user pool as other Sevaro apps)
- No API key on feedback Lambda for now (open in dev mode) — can lock down later

## Next steps
- Request SES production access to send to non-verified recipients
- Build admin management page (view/add/remove administrators)
- Test full flow on deployed Amplify (login → feedback list → status change → email)
- Consider adding feedback API key for production security
